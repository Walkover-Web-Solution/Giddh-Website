import PrivacyPolicy from "@/pages/privacy-policy"

const ukPrivacyPolicy = (path) => {
    return(
        <>
         <PrivacyPolicy path={path.path} />
        </>
    );
};
export default ukPrivacyPolicy;